#include <bits/stdc++.h> 
using namespace std; 
int main() 
{ 
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 
    // cout<<int('z'); 
    string s; 
    cin>>s; 
    int l = s.length(); 
    for (int i = 0; i < s.length(); i++) 
    { 

         return 0; 
    }